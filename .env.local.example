# This is an example file. For local development,
# copy this file to .env.local and fill in your details.
# DO NOT commit .env.local to version control.

# SMTP credentials for sending emails via nodemailer
# For Gmail, you must generate an "App Password" in your Google Account security settings.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=youremail@example.com
SMTP_PASS=yourapppassword
